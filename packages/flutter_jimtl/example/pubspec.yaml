name: jimtl_codegen_example
description: Intl utility to manage multi ARB files to manage flavors in your app
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl:
  flutter_jimtl:

dev_dependencies:
  build_runner: ^2.0.3
  jimtl_codegen:

dependency_overrides:
  flutter_jimtl:
    path: ../
  jimtl_codegen:
    path: ../../jimtl_codegen